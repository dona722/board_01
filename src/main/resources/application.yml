spring:
  jpa:
    show-sql: true
    properties:
      format_sql: true
      dialect: org.hibernate.dialect.MySQL8Dialect
    hibernate:
      ddl-auto: update
  datasource:
    url: jdbc:mysql://localhost:3306/kimdohun?useSSL=false&useUnicode=true&allowPublicKeyRetrieval=true
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: kimdohun
    password: kimdohun
  thymeleaf:
    prefix: classpath:/templates/
    suffix: .html
    cache: false

book:
  admin:
    password: "1234" 